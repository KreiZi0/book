class book {
	constructor(title, author, isbn){
		this.title = title;
		this.author = author;
		this.isbn = isbn
	}
		addedToLS(){

			console.log(this.title + this.author + this.isbn + " is added to LS");
	}
		
	
}